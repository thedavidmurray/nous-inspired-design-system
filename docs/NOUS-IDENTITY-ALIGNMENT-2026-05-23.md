# Nous-Inspired Page — Identity Alignment Notes (2026-05-23)

## Applied inline
1. **Hero copy** — bumped "300 prompts" to "600+ prompts. 6 modifications. One mascot." and added "locked character reference, front-loaded NOUS·HERMES wordmarks" to the description.
2. **Nav** — added `Character` link pointing to the new `#character` section.
3. **New SEC_03b** — "Character + Wordmark" gallery with 12 v10/v11 selects (Nous Girl + huge wordmark).
4. **Palette tokens** — expanded from 8 to 14 to include the Nous cyanotype family (`#031024`, `#063653`, `#1d6f8f`, `#a8c5d8`, `#bcd9e6`) and oxide red (`#a14e3f`). Reworded palette intro to call out the two families.

## Recommended but not applied (your call)

### Aesthetic DNA (SEC_02) — promote 1 Nous Girl frame
The current DNA gallery is rooted entirely in the original IMG_8154–8163 reference batch. Now that the Nous Girl is canonical character DNA, one of:
- `images/nous-girl-silkscreen-poster.jpg` (wide) or
- `images/nous-girl-storefront.jpg`

…belongs in SEC_02 so the "this is who we are" surface reflects the character system, not just the texture system.

### Generated Outputs (SEC_03) — refresh with v11
SEC_03 is the older v3 prompt set (oscilloscope, faraday cage, etc.). The v11 corpus is a stronger "look what's possible". Either:
- Add a small "v11 highlights" subsection
- Or rotate 2–3 of the v3 tiles for v11 equivalents

### Pipeline node (SEC_04) — update source list
Currently says "Midjourney v6.1 / v8.1". The v10/v11 batch ran on the alpha web app. Either say "MJ v6.1 → alpha" or just "Midjourney (current)".

### Add a SEC_03c: "How the Character Was Captured"
The brand-book extraction → cref-locked workflow is a story worth telling. Could be a 3-card block:
1. Extracted 8 refs from official NOUS brand booklet p.2 + p.9
2. Locked into MJ Image Prompts panel for ~70% character consistency
3. Front-loaded specific wordmark placement (`carved`, `silkscreened`, `vinyl-cut`, etc.) per prompt

This explains *why* the work has identity, not just *that* it does.

### Footer — add corpus link
"Full corpus in `generated/nous-mj/` (116 files, openly licensed)". Currently the gallery caption mentions it but there's no top-level pointer.

### Quickstart (SEC_10) — add a "Use the Character" subsection
The cref images live at `public/nous-girl-refs/` already. A 3-line "drop these into your MJ Image Prompts panel before generating to get character consistency" would close the loop for collaborators.

## Identity recap (what the page should communicate)

Three pillars, in order of weight:
1. **Texture pipeline** — original DNA, dithering modes, the deterministic 6-output story. (Strongly present.)
2. **Character system** — Nous Girl, locked ref, wordmark discipline. (Now present via SEC_03b; could be stronger.)
3. **Open corpus** — 600+ prompts, all the source images, MIT. (Present in hero but not reinforced elsewhere.)

The page leans heavily on (1) right now. The recent work has been (2) and (3). The applied changes start to balance this; the recommended ones would complete it.
